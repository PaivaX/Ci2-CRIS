<h1>Articles</h1>

<table>
  <thead>
  <tr>
    <th>Name</th>
    <th>Description</th>
  </tr>
  </thead>
  <tbody>
  <% @articles.each do |article| %>
    <tr>
      <%if article.title != nil %>
        <td><a href="/articles/<%= article.id %>"> <%= article.title %></a></td>
      <%else %>
        <td><a href="/articles/<%= article.id %>"> "sem títle" </a></td>
      <%end%>

      <%if article.type != nil %>
        <td><a href="articles/searchByType/<%=article.type%>"> <%=article.type %> </a></td>
        <%else %>
        <td>Article</td>
      <%end %>
    </tr>
  <% end %>
  </tbody>
</table>

<%= link_to "New Article", new_article_path, :class=>"btn btn-primary" %>

